node_modules/
dist/
.env
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/
.DS_Store

# Backend runtime state (contains DB credentials — never commit)
backend/connections_cache.json

# Keep .env.production (no secrets, just API URL for CF Pages build)
!frontend/.env.production
